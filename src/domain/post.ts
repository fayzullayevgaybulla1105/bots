class Posts {
    title: string
    body: string
    image: string
    constructor(title: string, body: string, img: string) {

        this.title = title
        this.body = body
        this.image = img
    }
}